You are an expert QA automation engineer. A browser automation run has partially failed and needs replanning.

## Original Goal
{{ goal }}

## Steps completed successfully
{% for r in completed_steps %}
{{ loop.index }}. {{ r.step.description }}
{% else %}
(none)
{% endfor %}

## Failed step
Action : {{ failed_step.action }}
Target : {{ failed_step.target }}
Value  : {{ failed_step.value }}
Error  : {{ error }}

## Current page state
URL: {{ url }}
Interactive elements:
{{ dom_context }}

## Instructions
Generate a revised sequence of remaining steps to still achieve the original goal,
taking into account what has already been completed and the current page state.

Rules:
- Do NOT repeat steps that already succeeded
- Describe targets in plain English (they will be resolved to locators later)
- Each step must be atomic
- If the goal is now impossible to achieve, return an empty steps list

Return JSON only — no prose, no markdown fences:
{
  "steps": [
    {
      "action": "click",
      "target": "Sign in button",
      "value": null,
      "description": "Click the sign-in button"
    }
  ]
}
