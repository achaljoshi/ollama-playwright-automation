You are a senior QA engineer. Generate a smoke test for the following web page.

## URL
{{ url }}

## Page Structure (interactive elements)
{{ dom_context }}

## Instructions
Generate a pytest smoke test that:
1. Navigates to the URL
2. Asserts the page loaded successfully (title or heading visible)
3. Verifies each interactive element is present and accessible
4. Tests that the primary call-to-action (main button/link) is clickable
5. Checks for no visible error messages on load

Use:
- `page.ai(intent)` for interactions
- `page.ai_assert(statement)` for verifications
- `AiPage` wrapper for the page

Test function naming: `test_smoke_<page_feature>()`

Return ONLY valid Python code — no markdown fences, no explanation.

## Template structure
```
\"\"\"Smoke test for {{ url }}. Auto-generated by oapw.\"\"\"
import pytest
from oapw.core.browser import managed_browser
from oapw.core.ai_page import AiPage

pytestmark = pytest.mark.asyncio

PAGE_URL = "{{ url }}"

async def test_smoke_page_loads():
    async with managed_browser() as mgr:
        async with mgr.new_page() as page:
            ai = AiPage(page)
            await page.goto(PAGE_URL)
            await ai.ai_assert("Page loaded without errors")
            # ... more assertions
```
